add_action('in_admin_header',function(){
/*in_admin_header
    global $wp_meta_boxes;
    $t ='editorial';
    if(array_key_exists($t, $wp_meta_boxes)){
		//echo '<pre>'.print_r($wp_meta_boxes,true).'</pre>';
        $o = $wp_meta_boxes[$t]['normal']['default']['editorial-meta-box'];
		unset($wp_meta_boxes[$t]['normal']['default']['editorial-meta-box']);
        $o['title'] = 'Group Actions';
       $wp_meta_boxes[$t]['normal']['default']['editorial-meta-box'] = $o;
    };
*/	
});
